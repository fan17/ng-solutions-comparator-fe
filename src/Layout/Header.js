import React from 'react';
import { Navbar } from 'react-bootstrap';

const Header = () => (
  <Navbar bg="primary" variant="dark">
    <Navbar.Brand href="#home">NG Solution Comparator</Navbar.Brand>
  </Navbar>
);

export default Header;
